type Theme = {
  icon: {
    first: string
    second: string
  }
  background: {
    first: string
    second: string
  }
}

type Score = {
  eScore: number
  bScore: number
  iScore: number
}

type Language = 'ja' | 'en'
